# 荣誉墙的数据是从哪来的

荣誉墙（`/honors/`）上那批记录不是手工敲进后台的，是 `manage.py import_honors`
一次导入的。这份文档说明来源、口径，以及要加新记录时该改哪里。

## 来源

`2025科协招新.pptx`（2025 年 9 月的招新宣讲 PPT，25 页 / 104 张内嵌图）。里面有
十几年的获奖证书照片。**这个 pptx 有 27MB，不入库** —— 和 `正面.jpg` 那几个本地
参考资料一样列在 `.gitignore` 里。要重新取的话：pptx 就是个 zip，图在
`ppt/media/`，每页文字在 `ppt/slides/slideN.xml` 的 `<a:t>` 里。

另有一张 2026 年的证书是单独给的（第九届嵌入式芯片与系统设计竞赛 全国一等奖）。

## 三条口径

**一、队员姓名一律部分脱敏，指导教师不脱敏。** 「李小明」→「李*明」（虚构名做示例），
两个字的只留姓，罗马字名保留姓。规则、工具与理由见 `scripts/mask_names.py` 与
`docs/PPT素材清单.md` 的「口径」一节。三条要记住的：

- **真名只存在 `.artsrc/honors-names.txt`，不入库。** 这个仓库是公开的，
  真名写进 `import_honors.py` 就等于发布了 —— 渲染时脱敏挡不住 git。
- **加记录之后跑 `python scripts/mask_names.py --scan`**，它拿底稿扫整个仓库；
  漏在文档或注释里的真名不会以任何形式报错。
- **证书图片也要遮姓名区域**（`build_certs.py` 里逐张标了坐标），
  否则图一贴上去等于没脱敏。

这一条和「把成员信息放到公开页面上必须本人同意」是同一件事的两种做法：团队页靠
`User.show_on_team` 让本人自己决定，这里靠脱敏把「谁」这个信息本身拿掉 ——
十几年前毕业的队员问不到，而「协会 2011 年拿过全国二等奖」这个事实不依赖于
把三个人的名字挂在公网上。

**二、软著不算「国家级」。** `Honor.Level.NATIONAL` 的语义是国赛获奖；软件著作权
登记证书是国家版权局发的凭证，不是竞赛奖项。混进去会让荣誉墙顶部那个「国家级 N
项」变成「国赛获奖 + 别的东西」。所以它归 `OTHER`，分量由标题里的「国家版权局」
交代。

**三、两条没有证书照。** 第十八届智能车独轮车组国一、TRIZ 杯创新方法大赛国一 ——
PPT 里那两张只有 309×433，放到证书墙上是一块糊斑。奖项信息照录（PPT 正文里有），
图留空。模型注释里本来就写着「证书照片是可选的加分项，不是骨架」。

## PPT 里的硬数据落在了哪几页

不是 `Honor` 记录，是写进模板的文案。**改数字要连这份表一起改**，否则下一个人无从
知道那几个数是哪来的。

| 数据 | 在哪一页 |
| --- | --- |
| 同校同赛事获奖对照（4 行表格） | 新生指南 `#intro`「成绩：拿同一所学校做对照」 |
| 历届保研率 100% / 83% / 80% / 78% | 同上，「毕业之后去了哪」 |
| 2024 年 **12 月**承办「红色电波」东北五校比赛 | 同上，「我们也自己办比赛」。PPT 正文写的「11 月」是错的，背景板与学院公众号都是 12 月 —— 见 `docs/PPT素材清单.md` |
| 培训四条承诺（面向全体电子类大一、实验室开放、上下学期分工、可以向任何一位学长请教） | 新生指南 `#training`「先把几件事说定」 |
| 2025 全国电赛「全校两项国一都是科协的」 | 首页「3 个理由」第三条；招新落地页「凭什么值得来」 |

对照表**只存在于新生指南一处**。落地页和首页各留一句最强的数字 + 指回指南的链接 ——
同一张表放两处，改一处忘一处是必然的。落地页的正文整块包在「有开放批次」的条件里，
而这些事实全年有效，所以权威副本必须在指南上。

口径上两处刻意的取舍：
- **「校内其他队伍」写明白了是什么**（哈工程除科协以外参加同一项赛事的队伍）。
  不写清楚的话「6 比 2」可以被读成任何意思。
- **保研率那段跟了一句「这不是承诺」**。四个数字并排放着很容易被读成「进来就能保研」，
  而它们是过去几届的事实，不是对下一届的许诺。
- PPT 里还有「22 级保外率 50%」，**没有采用** —— 「保外」要解释一句才懂，而这一段
  已经在讲四个数字了，再加一个不同口径的数会把它稀释掉。
- 三位学长学姐的详细履历**没有上站**（理由见下一节）。指南上只保留了协会层面的数据，
  个人成绩不是招新页该拿来当筹码的东西。

## 没有录进来的东西

PPT 里还有这些，**刻意没用**：

| 是什么 | 为什么不用 |
| --- | --- |
| 8 本教材封面、Vivado / Keil / Linux / Altium / PyTorch 商标、各赛事会徽 | 第三方版权物。元件图鉴用自绘 SVG 符号，正是为了避开这个 |
| 学生名单扫描件（含姓名 + 学号）5 张 | 隐私。学号是身份标识 |
| 微信群聊截图 2 张（含加分名单） | 隐私 + 聊天内容 |
| 几位同学的自拍与表情包梗图 5 张 | 未经本人同意的肖像，而且明显是内部玩笑 |
| 2025 年的旧招新 QQ 群二维码 | 站上现有的是当前群，放旧的会把人引错 |
| 三位学长学姐的完整履历（含均分、专业排名、保研 offer） | 学业成绩比姓名敏感得多。招新页只保留「XX 级学长，获 XX 奖」那一档 |

## 同一份 PPT 里还取了十张现场照

不是荣誉墙的内容，但来源相同，记在一起免得下次找不到。裁切参数在
`scripts/build_photos.py` 里，改构图改那里、重跑一次即可。

其中六张进的是**素材槽的 `fallback`**（`core/slots.py`），不是数据库 —— 理由见
`build_photos.py` 的文件头（`MediaSlot.image` 落在被 gitignore 的 `MEDIA_ROOT`，
本地传的图到不了生产）：

| 槽位 | 母图 | 是什么 |
| --- | --- | --- |
| `home.legacy.group` | `image1.jpeg` | 2024 年全员合影，约 60 人。分镜 02 末尾的整宽带（16/5，全出血） |
| `home.gallery.group` | `image44.jpeg` | 2025 TI 杯全国电赛出征合影，21 人。源图本来就是 4:3，一刀不用裁 |
| `home.gallery.award` | `image41.jpeg` | 2024 TI 杯颁奖台。只从两侧裁，横幅上的赛事名与奖次要留住 |
| `home.gallery.lab` | `image42.jpeg` | 赛场调试：指导老师 + 三名队员围着示波器。抓拍 |
| `recruit.hardware.bench` | `image26.jpg` | 工作台：两台 RIGOL（示波器上有波形）+ 一排开发板，同轴线还接着 |
| `recruit.software.debug` | `image27.jpeg` | 手持探头在测 FPGA 开发板，Tektronix 屏上是方波 |

另外四张是**模板里写死的 `<img>`**，因为那几个位置不缺图、也不该由站务换
（判据是 `core/slots.py` 那条「这里缺一张我们自己的照片吗」）：

| 位置 | 母图 | 是什么 |
| --- | --- | --- |
| 首页「3 个理由」主图 | `image25.jpg` | 实验台全景。549x686 的 4:5 竖裁，骑跨黑白分界线 |
| 首页方向卡 01 硬件 | `image23.jpeg` | 木板上的射频系统：TI LaunchPad + 自制模块 + SMA 互联 |
| 首页方向卡 02 嵌入式 | `image24.jpeg` | 射频宽带放大器，LCD 上有实时参数 |
| 首页方向卡 03 电赛 | `image43.jpeg` | 赛场上两名队员在看一块自制 PCB，TI 队服 |

**合影里有可辨认的人脸**，这是用户逐条确认过要用的（问过「现场照怎么处理」，答
「直接用」）。它和「把成员信息放到公开页面上必须本人同意」不冲突的地方在于：那条
约束管的是**把某个具体的人和他的姓名、联系方式、学号摆到页面上**，合影不指名任何
人，页面上也没有一个字的个人信息。要是有成员提出不想出现在合影里，换一张就是了 ——
改 `build_photos.py` 里那一行。

`recruit.training.session` **刻意留空**：手上这批照片里没有培训课现场，最接近的是
赛场调试（已经用在 `home.gallery.lab`）。按 `fallback` 那段说明，没有合适的自有素材
就留空显示占位框，比放一张不相干的图诚实，而且它会继续留在驾驶舱的缺图清单里提醒
去拍。它现在也是登记表里**唯一**一个没有 `fallback` 的图片槽 —— `core/tests.py` 的
`an_empty_slot_key()` 拿它当「空态长什么样」的样本，真把它填上时那几条断言会带着
说明红掉，到时候按提示在那里造一个临时 SlotSpec。

## 要加新记录时改哪里

1. 证书原图放 `.artsrc/certs/`（不入库，和美术资产母图同一个约定）
2. 在 `scripts/build_certs.py` 的 `CERTS` 里加一行（输出名, 源文件名），跑一次
   → 产物落在 `app/news/data/honors/*.webp`，**这个目录要提交**
3. 在 `app/news/management/commands/import_honors.py` 的 `HONORS` 里加一行
4. `python manage.py import_honors --dry-run` 看一眼，再去掉 `--dry-run`
5. **跑 `python scripts/check_content_fonts.py`** —— 人名容易带 GB2312 之外的字，
   缺字会静默回退系统黑体。它会告诉你该往 `build_fonts.py` 的 `CONTENT_EXTRA`
   里加什么（`淏` 就是这么发现的）
6. 生产上部署完再跑一次 `import_honors`（命令幂等，重跑不堆数据）

## 为什么产物要提交进仓库

`Honor.certificate` 是 `ImageField`，落在 `MEDIA_ROOT` 下，而 `app/media/` 是
gitignore 的 —— 本地导入的图**到不了生产**。所以处理后的 webp 放在
`app/news/data/honors/`（跟着代码走），由导入命令喂给 `ImageField`。
13 张合计约 1.1MB（原图 5.8MB）。
