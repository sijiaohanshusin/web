# 成果体系正式部署验收

## 发布版本与范围

2026-09-06 将应用从 `8e7c4de` 更新到 `ede30a6b7586485a7477b5f3ccb3e4cc7f50e86d`。作品墙自助发布、荣誉自助发布、共同参与者、邀请确认、历史认领和站务核验已正式启用。后续手册提交不作为新的应用版本冒充上线。

上线前主分支 CI `34041737640` 通过，功能分支同版本 CI `34041347771` 通过；PostgreSQL 完整 728 项测试无跳过，浏览器、插件及论坛集成通过。

## 备份与迁移

- 发布备份标识 `release-20260906T152552Z-ede30a6`。网站与论坛数据库、配置、原代码、上传和静态资源均已备份；备份目录仅 root 可读，SHA-256 校验通过。
- 将网站 SQL 恢复到临时数据库，使用新镜像完成迁移演练后删除临时数据库。生产数据库只执行正式迁移，不运行测试或创建演示账号。
- 先安装荣誉证书与作品图片的 Nginx 私有目录保护，再启用新应用；Nginx 配置检查通过。
- 基于原镜像复用现有运行环境，只替换应用代码。Python 与所有已安装依赖的前后列表完全相同，没有升级基础镜像或重新安装依赖。
- 只重建主站应用容器。论坛和数据库容器 ID、启动时间前后完全相同；应用重启计数为 0，无 OOM，定时备份保持运行。
- 保留 `heuesta-app:compatible-ede30a6` 作为理解新数据的兼容基线。新数据产生后不能直接切回不理解成员荣誉的旧应用，不通过恢复旧数据库覆盖新记录。

## 正式站只读验收

- `ops/verify.sh` 全部通过：应用、部署 SHA、图片字体哈希、静态缓存与压缩、容器状态。
- `scripts/check_live.py` 通过：12 个公共页面、125 个唯一图片地址，其中 35 张荣誉证书；没有断图或指向私有上传目录的公开图片。
- 成果管理未登录时跳到登录；内部手册目录、文章和图片对游客返回 404，不泄露内容。
- 直接访问私有荣誉或作品上传目录返回 403；内部文件地址不可直接访问；不存在的证书返回 404 且禁止缓存。
- 作品墙、荣誉墙、成果帮助正文均取得新内容，公网 `EO-Cache-Status: MISS`、`Age: 0`；动态内容 `no-store`。没有调用 CDN 清理接口，不声称执行了清理。
- HTTP 返回 HTTPS 301；有效证书验证通过。浏览器检查作品墙、荣誉墙和成果帮助的 1440px/390px 页面，六个画面均无横向溢出，字体和图片正常。
- 本机一次浏览器连接被关闭，重试后完成；Windows curl 曾因本机吊销检查端点不可达报错，Python、Chromium 与源站 curl 的证书校验均成功，未关闭 TLS 校验。

证据位于本地 `.shots/release-ede30a6/`；服务器发布备份内保留迁移演练日志、校验和与运行环境对比。不包含公开可下载的数据库或配置备份。

## 手册正式换发

三册改为 `*-first-use-achievements-release`，对应正式站 `ede30a6`。新增独立正式文件名，避免覆盖读者正打开的旧候选 PDF。目录在 `docs/help/README.md`，产物保持本地交付，管理册不进入公开下载目录。

重新渲染 60 页；三张封面的版本说明已目视复核，其余 57 页与此前逐页验收的 PNG 完全相同。改为正式文件名后，再次渲染的 60 页与上一轮逐字节一致。结构检查通过，页数为 16/27/17，54 处截图，目录和反馈导航有效。

修复手册校验器把“尚未发布的草稿”当作网站未上线的误报；封面的未发布标签及正文的“待上线/尚未上线/网站候选版本”仍会被阻止，新增 4 项回归测试。帮助中心 21 项测试通过。

## 仍需单独处理

- Django `check --deploy` 的反嵌套和 HTTPS 跳转提示由 Nginx 实际提供；HSTS 当前未启用，保留为独立安全配置工作，不在本轮无评估地改动。
- 网页帮助目录中成果与论坛暂共用“10”编号，属于非阻断的导航细节；后续整理显示序号，不修改稳定文章地址或手册任务编号。
- 作品墙为空是尚无真实成员公开作品，不以虚构账号填充。

## 上线后反馈

用户反馈点击核验通过进入空白 403。日志确认命中服务端禁止自审规则，而非 CSRF 或代理故障。规则继续保留；页面不应给申请人展示可执行的审核表单，旧标签页提交应返回解释。新增回归测试先复现 4 个失败断言，再修复页面与提交反馈；正式修复验收另记。
