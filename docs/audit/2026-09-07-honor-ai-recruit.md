# 荣誉识别与招新体验候选验收

## 基线与发布边界

- 日期：2026-09-07；隔离分支 `codex/honors-ai-recruit-20260907`，基线 `6ed8255`。
- 本轮没有部署正式站，没有调用真实阿里 API，没有使用或保存聊天中提供的临时 Key。
- 两个独立改造：荣誉证书多模态辅助填写；导航选中态、招新小机器人及手机锚点兼容。
- AI 默认关闭。完成真实模型、免费额度有效期与阿里侧“用完即停”验收后才可启用；不更改会员资格、认领、公开同意或论坛。

## 已完成的本地验证

| 检查 | 结果与证据 |
| --- | --- |
| 完整 Django 回归 | 756 项运行通过，8 项 PostgreSQL 专属检查在 SQLite 下跳过；隔离 PostgreSQL CI 另行通过完整回归、迁移及预检 |
| 识别接口 | 本人归属、CSRF、资格、空表单先传图、重复任务、日限额、超时、24小时结果清理、恶意识别内容、免费额度熔断均有测试 |
| 浏览器完整流程 | `scripts/check_honor_ai.py`：真实本地上传/任务/轮询接口，模拟模型响应；核对人工输入保护、差异选择、撤销、服务失败恢复、保存预览、发布与撤回 |
| 隐私 | 模拟识别不改荣誉草稿、不自动选择公开证书、参与者用户名不自动填写；访客发布前及撤回后读取证书均404 |
| 移动与可访问性 | 1440、768、390、320px 无横向溢出；键盘重播、减少动效、硬件/软件/培训锚点、关闭 JavaScript 后可见内容通过 |
| 导航回归 | 先复现资料站学习资源组未选中，再修复；新生指南不再把招新标为当前页 |
| 识别完成与删除竞态 | `793970d` 先补测试复现删除证书后的任务复活，再改为条件更新；已删除记录不重新插入 |
| 本地发布预检 | `scripts/preflight.py --candidate` 通过；本机缺少私有姓名底稿，相关隐私扫描跳过；未验证 Docker 构建 |
| 性能回归 | CI 发现独立导航 CSS 造成首页第5张阻塞样式；保持原4张上限，合并进 `core.css`。本地和最终 CI 的 `check_perf.py` 全部通过，未提高预算 |
| 完整 CI | `e7f21eb` 的 PostgreSQL、浏览器与插件、论坛集成三组全部通过，包含注册、成果、荣誉识别、管理、手册截图、性能和公开展示回归 |
| 首次上手手册 | 独立候选册29页、28张图，目录/任务速查/54个内部链接检查通过；逐页视觉检查，新增识别与核对章节、失败处理和反馈方法 |

浏览器截图与检查记录在 `.shots/honor-ai/`。仅使用隔离测试账号和虚构证书，测试结束清理；不是正式站真实模型识别的证据。

## 待完成的发布门槛

1. 自动化发布门槛已通过：隔离 PostgreSQL 完整测试、重复任务单次预留、删除图片不绕过单并发，以及浏览器、论坛和性能检查。最终结果见 [e7f21eb 完整验证](https://github.com/sijiaohanshusin/web/actions/runs/34135980269)。运行环境和真实外部服务验收仍不可由 CI 代替。
2. 配置北京业务空间 ID、受保护环境中的通用 Key、真实额度到期时间；确认模型余额和“免费额度用完即停”。联调候选为 `qwen3.7-flash` 视觉模型，不走 OCR 专用任务。
3. 用获授权且已脱敏的完整/模糊/多人/省赛证书做小样本真实验证，记录字段正确率、空值处理、实际 Token 和响应时间。模拟测试不能证明 Key 可用、免费或识别准确。
4. Android QQ 内置浏览器及 iOS 真机复测。当前只有 Chromium 移动模拟，不能宣称已修复尚未复现的真机黑屏。
5. 上线前备份、演练迁移、构建应用及静态资源，再分别发布；只更新主站及可选识别进程，不重启论坛。公网权限和缓存验收后再结束发布。

## 手册交付

以 `docs/help/dist/2026-09-07/member-first-use-honor-ai-preview` 为文件名前缀的 DOCX、PDF、Markdown 为候选体验版，原正式手册未覆盖。源内容是 `app/helpcenter/content/member/achievements.md`，编排覆盖文件是 `scripts/manuals/first_use_honor_ai.json`。新图日期2026-09-07，原图沿用其实际拍摄日期。

接入、排错和回退步骤见 [荣誉证书AI接入](../荣誉证书AI接入.md)。
