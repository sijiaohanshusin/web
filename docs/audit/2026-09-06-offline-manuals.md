# 2026-09-06 离线手册复核

## 对照范围

正式站应用版本 `8e7c4de`；工作树 `b541159` 在此基础上只有上线巡检记录。复核三册首次上手手册，保留有效的注册、审核、活动和论坛截图，不重新制造生产演示数据。

## 发现与修订

| 问题 | 处理 |
| --- | --- |
| 封面与维护说明仍称候选版本未上线 | 改为已上线版本和 2026-09-06 核验日期，构建提交号只留在证据元数据 |
| 成员展示和职位帮助元数据仍称未上线 | 更新版本描述，保留原截图核验日期 |
| 三册没有可操作的统一 BUG 反馈流程 | 增加入口、提交成功判断、原对话追问、匿名限制和网站不可用时的求助方法 |
| 老会员册只有名片与发布，没有作品实操 | 增加作品名称封面、关联项目、卡片代表作、个人页模块、图集链接和素材管理 |
| 素材选择说明暗示额外确认按钮 | 改为点击素材直接应用并返回原位置 |
| 手册只能按目录顺序查阅 | 增加任务速查、操作路线、真实入口、每页反馈快捷跳转与打印页码 |
| 图集链接合页导致孤立尾段 | 拆出链接检查页，保留可读字号和截图，不以缩小内容强求页数 |
| 新增入口把论坛绝对地址重复拼接 | 区分站内相对路径与论坛 HTTPS 地址，并保留外链白名单检查 |
| 作品相关入口统一打开个人页布局 | 为精选作品、卡片内容、图集链接分别配置直达分区地址 |
| Word 导出把内部标题链接解释为外部 `h` 地址 | 先加入 PDF 链接检查复现失败；改为显式本地 HYPERLINK 域，重新导出后所有目标页与页脚检查通过 |

## 验证边界

- 正式反馈页 DOM 与公开入口已核对；未提交生产反馈，不改生产账号和公开内容。
- 同版隔离演示实际完成添加作品、选封面、卡片精选、启用模块、添加图集与链接、自动保存后重新进入核对，以及查看本人反馈对话。
- 关联项目地址与反馈重新打开规则同时对照服务端和模板；未声称对生产账号进行完整写入测试。
- 新增 3 项文档回归，帮助中心 19 项测试通过。完整 Django 661 项通过，跳过 2 项 PostgreSQL 专属测试；本轮没有修改数据库结构，未重跑 PostgreSQL 环境。
- 新截图只含虚构身份；没有密码、邮箱验证码、真实会员联系方式或服务器信息。保留图源日期，裁切与标注不替换网站控件。

## 交付验收

| 册别 | 页数 | 截图数 | 交付文件前缀 |
| --- | ---: | ---: | --- |
| 招新注册 | 15 | 12 | `recruit-first-use` |
| 老会员 | 20 | 19 | `member-first-use` |
| 网站管理（内部使用） | 15 | 13 | `admin-first-use` |

- 交付位于 `docs/help/dist/2026-09-06/`，每册包含 DOCX、PDF、Markdown。保留原日期目录外的旧副本，不覆盖用户打开的旧 PDF。
- 共 50 页逐页打开 PNG 检查：无孤立任务尾段、文字遮挡、异常分页、缺字或跨页表格；关键控件与新增作品截图可读。封面、目录、速查与每页反馈快捷入口一致。
- 完成内部链接修复后再次渲染。50 页 PNG 的 SHA-256 与已逐页审阅版本全部相同，证明链接修改未改变视觉版面。
- PDF 的章节目录到达全部任务；正文每页均有到目录、速查和反馈章的有效目标；外链只允许主站和论坛 HTTPS。结构检查三册均为零错误。
- 最后一次帮助中心测试 19 项通过；完整回归结果及 PostgreSQL 未重跑边界见上文。
- 本轮创建的隔离账号、展示数据库记录和演示反馈已精确清理。没有修改正式成员、反馈、论坛或站点配置，也没有自动发布任何展示。
- 正文更新保存在帮助中心源稿；本轮不部署网站功能或网页正文。旧全文副本只作历史参考，不作为当前首次引导发放。

## 追加修订 作品提交

按后续要求，仅重新生成 `member-first-use`，上述三册初次验收记录保留，不覆盖原证据。

- 新增任务 16“向协会提交作品”，位于第 19 页；会员册现为 21 页、20 张截图。第 3 页速查增加“作品提交／协会作品墙”，反馈章节顺延为第 20、21 页。
- 正式站 `/works/` 只读核验仍为空，当前代码没有普通会员作品墙提交表单。新章写明材料准备、共同作者同意、联系站务、反馈联系备用入口与发布后核对，不虚构自助上传或审核状态。
- 保留个人展示作品章节；明确个人展示不自动登上协会作品墙，提交材料不授予项目编辑权限，也不自动公开。
- 新增正式站空状态截图，只裁切已有画面，不添加按钮或虚构作品。活动旧图仍标 2026-09-05，不因文章重新核验而改写拍摄日期。
- 先以新增章节与流程边界测试复现缺项，再补正文与索引；帮助中心 20 项测试通过。此次仅文档与构建工具更新，未再次运行全站及 PostgreSQL 回归。
- 最新 DOCX 经文档技能渲染器转换，21 页 PNG 全部逐页检查，任务未跨页、索引完整、截图及图注可读。PDF 目录、速查、反馈快捷跳转与外链结构检查全部通过。
- 会员 DOCX SHA-256：`575c460cbf1113551706f092c891bea36094f3bb6c24908b6557c37c099116b0`；PDF SHA-256：`bfd102442a66c2cecd54f611f7eb19aa3e9afee5082bb6f71f2be2cef33fdb86`。
- 招新册与管理册的 DOCX、PDF、Markdown 共 6 个文件逐一对比 SHA-256 未变化，两册证据记录亦未变化。没有生产写入、账号变更或网站部署。
